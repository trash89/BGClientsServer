{
  "version": 2,
  "name": "bgclients",
  "builds": [
    { "src": "*.html", "use": "@vercel/static" },
    { "src": "client/*.*", "use": "cd client && npm install && npm run build" },
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/server.js" }]
}
